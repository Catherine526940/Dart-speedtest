before_script:  
 - export PATH=`pwd`/flutter/bin:$PATH
 - chmod a+rwx /home/flutter/bin/cache/lockfile
 - chmod a+x ./android/gradlew
 
stages:
    - build

dev:
   stage: build
   script:
     - cd android
     - gradlew app:assembleDebug
