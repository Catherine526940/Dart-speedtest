import 'package:flutter/material.dart';

class HistoryDetPage extends StatelessWidget {
  const HistoryDetPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('historydetailpages!!!');
  }
}